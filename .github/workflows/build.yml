name: build

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4
      - name: Test
        run: swift test
      - name: Build app
        run: zsh scripts/build-app.sh
      - name: Privacy audit
        run: zsh scripts/audit-public.sh
